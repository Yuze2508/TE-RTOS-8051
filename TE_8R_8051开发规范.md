# TE_8R_8051 开发规范

> 适用版本: 工作区模式 (idata 工作区 + xdata 用户栈区) + TCB 冷热分离 + 自适应等待位图 + 三层栈溢出检测 + 配置分离 + 谓词宏 + 汇编-C契约
> 目标芯片: 8051 内核全系列单片机 (传统 12T / 增强型 1T 均可)

## 目录

- [§1 系统配置约束](#s1)
  - [1.0 配置文件](#s1-0)
  - [1.1 编译期硬限](#s1-1)
  - [1.2 运行时校验](#s1-2)
  - [1.3 配置建议](#s1-3)
- [§2 任务开发规范](#s2)
  - [2.1 任务函数结构](#s2-1)
  - [2.2 任务编号](#s2-2)
  - [2.3 优先级分配](#s2-3)
  - [2.4 栈空间预算](#s2-4)
- [§3 启动流程规范](#s3)
- [§4 延时与让位规范](#s4)
  - [4.1 TE_8R_8051_Delay](#s4-1)
  - [4.2 TE_8R_8051_yield](#s4-2)
- [§5 邮箱规范](#s5)
  - [5.1 容量与格式](#s5-1)
  - [5.2 语义: 最新值有效](#s5-2)
  - [5.3 三个 API 的选择](#s5-3)
  - [5.4 邮箱 + 延时的典型模式](#s5-4)
- [§6 互斥量规范](#s6)
  - [6.1 基本用法](#s6-1)
  - [6.2 五条纪律](#s6-2)
  - [6.3 纪律 4 详解: 禁止嵌套持有](#s6-3)
  - [6.4 优先级继承行为](#s6-4)
  - [6.5 等待关系存储](#s6-5)
  - [6.6 TE_8R_8051_mutex_try_acquire](#s6-6)
- [§7 临界区规范](#s7)
  - [7.1 三件套用法](#s7-1)
  - [7.2 纪律](#s7-2)
  - [7.3 临界区内禁止事项](#s7-3)
- [§8 ISR 规范](#s8)
  - [8.1 可调用的 API](#s8-1)
  - [8.2 禁止调用的 API](#s8-2)
  - [8.3 ISR 存储模型](#s8-3)
  - [8.4 定时器 0 保留](#s8-4)
- [§9 存储模型规范](#s9)
- [§10-A TCB 冷热分离架构](#s10A)
  - [10A.1 设计动机](#s10A-1)
  - [10A.2 结构体定义](#s10A-2)
  - [10A.3 数组声明](#s10A-3)
  - [10A.4 汇编访问方式](#s10A-4)
  - [10A.5 RAM 开销（4 任务配置）](#s10A-5)
- [§10 栈溢出诊断](#s10)
  - [10.1 三层检测机制](#s10-1)
  - [10.2 诊断变量](#s10-2)
  - [10.3 可靠性边界](#s10-3)
  - [10.4 停机后指示](#s10-4)
- [§11 空闲任务规范](#s11)
- [§12 禁止事项汇总](#s12)
- [§13 典型任务模板](#s13)
  - [13.0 完整创建与启动示例](#s13-0)
  - [13.1 周期采样任务](#s13-1)
  - [13.2 互斥保护任务](#s13-2)
  - [13.3 邮箱消费任务](#s13-3)
  - [13.4 ISR 投递邮箱](#s13-4)
- [§14 任务状态谓词宏](#s14)
  - [14.1 四个谓词](#s14-1)
  - [14.2 使用场景](#s14-2)
  - [14.3 纪律](#s14-3)
- [§15 汇编-C契约](#s15)
  - [15.1 编译时断言](#s15-1)
  - [15.2 汇编常量同步](#s15-2)
- [§16 源文件清单](#s16)


## 一、系统配置约束
<a id="s1"></a>

### 1.0 配置文件
<a id="s1-0"></a>

所有用户可调参数集中在 `TE_8R_8051_config.h`，初学者只需关注这一个文件。

| 文件 | 角色 | 谁改？ |
|------|------|--------|
| `TE_8R_8051_config.h` | 用户可配置参数 | 用户 |
| `TE_8R_8051_config.inc` | 汇编侧常量（与config.h手动同步） | 用户（改config.h时同步改） |
| `TE_8R_8051.h` | 公开API声明 | 不改 |
| `TE_8R_8051_internal.h` | 内部实现细节 | 不改 |

#### 各文件内容与修改指引

**`TE_8R_8051_config.h`** — 用户唯一需要修改的头文件：

| 宏 | 含义 | 典型值 |
|----|------|--------|
| `FOSC` | MCU 主频 (Hz) | `24000000UL` |
| `TICK_MS` | 每 Tick 毫秒数 | `1` |
| `TE_8R_8051_MAX_TASKS` | 最大任务数 | `4` |
| `TE_8R_8051_MAILBOX_CAPACITY` | 邮箱容量 (字节) | `4` |
| `TE_8R_8051_AGING_STEP` | 饥饿老化步长 | `32` |
| `TE_8R_8051_MAX_WORKSPACE` | idata 工作区大小 | `80` |
| `TE_8R_8051_MAX_TIMERS` | 软件定时器数量 | `4` |
| `TE_8R_8051_STACK_MAGIC` | 栈哨兵魔数 | `0xFE` |

**`TE_8R_8051_config.inc`** — 汇编侧常量，改 `config.h` 后必须同步改此文件：

| 汇编常量 | 对应 config.h 宏 | 当前值 |
|----------|-----------------|--------|
| `TE_8R_8051_STACK_MAGIC EQU` | `TE_8R_8051_STACK_MAGIC` | `0FEH` |
| `TE_8R_8051_MAX_TASKS EQU` | `TE_8R_8051_MAX_TASKS` | `4` |

**`TE_8R_8051.h`** — 公开 API，包含：
- 硬件 SFR 声明：通过 `#include <STC15F2K60S2.H>` 引入芯片寄存器定义
- 所有内核 API 声明 (`Init`, `Start`, `Delay`, `yield`, `Task_Create`, 邮箱, 互斥量, 定时器)
- 数据结构定义 (`tcb_hot_t`, `tcb_cold_t`, `mutex_t`, `timer_t`)
- 宏定义 (`PRIO_OF`, `LEN_OF`, `MAKE_PRIO`, 临界区三件套, 帧尺寸常量)
- 栈溢出诊断变量声明

**更换目标芯片时**：需修改 `TE_8R_8051.h` 第 15 行的 `#include <STC15F2K60S2.H>`，替换为你所用芯片的 SFR 头文件（如 `<STC8H.H>`、`<REG52.H>` 等）。该头文件由 Keil 编译器的 INCLUDE 路径提供，不在本工作区中。确保新头文件至少定义了以下 SFR：`SCON`、`SBUF`、`TI`、`EA`、`TMOD`、`TH0`、`TL0`、`TR0`、`TH1`、`TL1`、`TR1`、`T2L`、`T2H`、`SP`、`DPTR`（`DPH`/`DPL`）、`ACC`、`B`、`PSW`、`P0`~`P4`。增强型 8051 还需 `AUXR`。

**移植到 12T 传统 8051 时**，需手动修改两处：

1. **Timer0 重装载值**：`TE_8R_8051.h` 中的 `TIMER0_RELOAD` 宏默认按 1T 模式计算：
   ```c
   #define TIMER0_RELOAD (65536UL - (FOSC * TICK_MS / 1000))
   ```
   12T 模式下每个机器周期为 12 个时钟周期，需改为：
   ```c
   #define TIMER0_RELOAD (65536UL - (FOSC / 12UL * TICK_MS / 1000))
   ```

2. **AUXR 寄存器**：`TE_8R_8051_core.c` 中 `TE_8R_8051_Init()` 里的 `AUXR |= 0x80;` 将 Timer0 设为 1T 模式。传统 8051 无 AUXR 寄存器，Timer0 默认即为 12T 模式，**需删除此行**，否则会写入不存在的 SFR 导致未定义行为。

**`TE_8R_8051_internal.h`** — 内部实现细节，包含：
- 任务状态谓词宏 (`IS_TOMBSTONE`, `IS_BLOCKED`, `IS_READY`, `IS_SLEEPING`)
- 内核数据声明 (`current_task`, `yield_skip`, `task_mailbox`, `timers`)
- 汇编接口声明 (`start`, `task_switch`)
- 编译时断言 (TCB 大小 = 5, ISR 帧大小 = 17)
- **用户不应修改也不应包含此文件**

**修改纪律**：改 `TE_8R_8051_config.h` 中的值时，必须同步修改 `TE_8R_8051_config.inc`！特别是 `TE_8R_8051_MAX_TASKS` 和 `TE_8R_8051_STACK_MAGIC`，它们直接影响汇编的栈溢出检测和TCB数组访问。

### 1.1 编译期硬限
<a id="s1-1"></a>

| 配置宏 | 约束 | 违反后果 |
|--------|------|----------|
| `TE_8R_8051_MAX_TASKS` | ≥ 1 且 ≤ 25 | 编译报错 (`#error`)；上界25因data区容量限制（tcb_hot[]需MAX_TASKS×5+5字节） |
| `TE_8R_8051_MAX_WORKSPACE` | ≥ `TE_8R_8051_INIT_FRAME_SIZE` (15) | 编译报错 (`#error`) |
| `TE_8R_8051_MAILBOX_CAPACITY` | 1 ~ 15 | 编译报错 (`#error`)，4 位信长字段硬限 |
| `TE_8R_8051_AGING_STEP` | ≠ 0 | 编译报错 (`#error`)；为0时调度器除零未定义行为 |
| `FOSC × TICK_MS / 1000` | ≤ 65535 | 编译报错 (`#error`)；超出16位定时器范围，重装载值溢出回绕 |

### 1.2 运行时校验
<a id="s1-2"></a>

| 校验项 | 时机 | 违反后果 |
|--------|------|----------|
| `id` 越界 | `TE_8R_8051_Task_Create` | 静默拒绝 (return) |
| `prio = 0` | `TE_8R_8051_Task_Create` | 自动升为 1 |
| `prio > 15` | `TE_8R_8051_Task_Create` | 钳位到 15 |
| `stack_size > TE_8R_8051_MAX_WORKSPACE` | `TE_8R_8051_Task_Create` | 静默拒绝 (return) |
| `stack_size < TE_8R_8051_MIN_STACK_SIZE` (33) | `TE_8R_8051_Task_Create` | 静默拒绝 (return) |
| `ticks ≥ 0xFFFE` | `TE_8R_8051_Delay` | 自动兜底到 `0xFFFD` |
| `id` 越界 / `len` 非法 | `TE_8R_8051_Send_Mailbox` / `TE_8R_8051_Post_Mailbox` | 返回 0 |
| `m->owner != current_task` | `TE_8R_8051_mutex_release` | 返回 0 |

### 1.3 配置建议
<a id="s1-3"></a>

- `TE_8R_8051_MAX_WORKSPACE` 应取所有任务 `stack_size` 的最大值，向上取整到合理值
- 开发期可读取 `TE_8R_8051_workspace_peak` 确认配置是否合理
- `TE_8R_8051_AGING_STEP` 默认 32：老化最高抬到 1+7=8，不会侵入实时档 (10~15)

---

## 二、任务开发规范
<a id="s2"></a>

### 2.1 任务函数结构
<a id="s2-1"></a>

```
void task_xxx(void) large    ← 必须声明为 large
{
    while (1) {               ← 必须是死循环
        ...
        TE_8R_8051_Delay(n);      ← 必须有让出 CPU 的机制
    }
}
```

**规则**:

- 任务函数**必须**为死循环 (`while(1)`)，不得返回
- 任务函数**必须**声明为 `large`，局部变量由链接器在 xdata 中静态分配，idata 栈需求极小
- 任务函数**禁止**使用 Keil 的 `using` 关键字切换寄存器组，必须使用第 0 组
- 每个任务**必须**有让出 CPU 的机制（`TE_8R_8051_Delay` / `TE_8R_8051_yield` / 阻塞在互斥量），否则同优先级或低优先级任务永远无法运行

### 2.2 任务编号
<a id="s2-2"></a>

- 合法范围: `0` ~ `TE_8R_8051_MAX_TASKS - 1`
- 编号即物理槽位，不可重复，重复创建会覆盖前一次的全部状态
- 空闲任务槽位 (`TE_8R_8051_MAX_TASKS`) 对用户不可见，不可操作

### 2.3 优先级分配
<a id="s2-3"></a>

优先级档位 (0~15, 4 位上限):

| 档位 | 语义 | 典型任务 |
|------|------|----------|
| 0 | 空闲任务独占 | 用户不可用，传 0 自动升为 1 |
| 1 ~ 3 | 后台统计/日志 | 计数上报、看门狗喂食 |
| 4 ~ 9 | 常规业务 (主力区) | 采样、控制、通信 |
| 10 ~ 14 | 时间敏感型 | 硬件响应、紧急保护 |
| 15 | 最高实时档 | 临界时限任务 |

**规则**:

- 优先级 0 为空闲任务独占，用户任务传 0 会被自动升为 1
- 超过 15 会被钳位到 15，不报错
- 饥饿老化最高将有效优先级抬到 8，**不会侵入实时档** (10~15)，硬实时任务的确定性不受影响

### 2.4 栈空间预算
<a id="s2-4"></a>

#### 帧尺寸常量

| 常量 | 值 | 含义 |
|------|-----|------|
| `TE_8R_8051_INIT_FRAME_SIZE` | 15 | 初始帧：PCL+PCH+13寄存器 |
| `TE_8R_8051_ISR_FRAME_SIZE` | 17 | 中断帧：PC(2)+13寄存器+LCALL返回地址(2) |
| `TE_8R_8051_MIN_STACK_SIZE` | 33 | 最小栈：INIT_FRAME+ISR_FRAME+哨兵(1) |

#### large 模型 (推荐)

局部变量在 xdata，idata 栈只压入返回地址 (每层 2 字节):

```
stack_size ≥ 33 (初始帧15 + 中断帧17 + 哨兵1) + 2 × 最大调用嵌套深度
```

即 `stack_size ≥ TE_8R_8051_MIN_STACK_SIZE + 2 × D`

示例: 最深调用 3 层 → `stack_size ≥ 33 + 2×3 = 39`，建议 42

#### small 模型 (不推荐用于任务)

局部变量在 idata 栈，开销大幅增加:

```
stack_size ≥ 33 (初始帧15 + 中断帧17 + 哨兵1) + 2 × 最大调用嵌套深度 + 局部变量总和
```

示例: 最深调用 3 层，每层最多 4 字节局部变量 → `stack_size ≥ 33 + 2×3 + 4×3 = 51`

**规则**:

- `stack_size` 不得小于 `TE_8R_8051_MIN_STACK_SIZE` (33)，否则创建被拒绝
- `stack_size` 不得超过 `TE_8R_8051_MAX_WORKSPACE`，否则创建被拒绝
- 栈区声明在 xdata: `unsigned char xdata stack_xxx[42];`
- 栈溢出时三层检测（L1/L2/Tick）触发停机，诊断信息可通过调试器读取

---

## 三、启动流程规范
<a id="s3"></a>

```
void main(void)
{
    硬件初始化();              ← UART、GPIO 等

    TE_8R_8051_Init();            ← 内核初始化 (配置定时器0，创建空闲任务)

    TE_8R_8051_mutex_init(&...);  ← 互斥量初始化 (在 TE_8R_8051_Init 之后)

    TE_8R_8051_Task_Create(0, ...);  ← 创建所有用户任务
    TE_8R_8051_Task_Create(1, ...);
    ...

    TE_8R_8051_Start();           ← 启动调度，永不返回
    while (1);                 ← 不可达，仅防编译警告
}
```

**规则**:

- `TE_8R_8051_Init()` 必须在所有 `TE_8R_8051_Task_Create()` 之前调用
- 所有 `TE_8R_8051_Task_Create()` 必须在 `TE_8R_8051_Start()` 之前完成
- `TE_8R_8051_Start()` 永不返回，其后的代码不可达
- 互斥量初始化 (`TE_8R_8051_mutex_init`) 须在 `TE_8R_8051_Init()` 之后、任务使用之前

---

## 四、延时与让位规范
<a id="s4"></a>

### 4.1 TE_8R_8051_Delay
<a id="s4-1"></a>

```
TE_8R_8051_Delay(ticks);
```

| ticks 值 | 行为 |
|-----------|------|
| 0 | 无睡眠直接切换，无落选惩罚，调度器可能再次选回当前任务 |
| 1 ~ 65533 | 睡眠指定 Tick 数，减到 0 自动苏醒 |
| ≥ 65534 | 自动兜底到 65533 (0xFFFE 和 0xFFFF 为内核保留值) |

**特性**: 收到邮件时会被提前唤醒，实际睡眠时间可能短于 `ticks`

### 4.2 TE_8R_8051_yield
<a id="s4-2"></a>

```
TE_8R_8051_yield();
```

**语义**: 让出一拍 CPU，**保证**时间流向其他就绪任务

**与 TE_8R_8051_Delay(0) 的区别**:

| | `TE_8R_8051_Delay(0)` | `TE_8R_8051_yield()` |
|---|---|---|
| 调度器可能选回自己 | 是 | **否** (本轮强制落榜) |
| 适用场景 | 无偏好让出 | 确定性让位 |

**规则**:

- `TE_8R_8051_yield()` **禁止**在 ISR 中调用
- 可无限次调用，无副作用累积
- 典型用途: 长计算任务在分割点之间插入，为低优先级任务提供确定性服务窗口

---

## 五、邮箱规范
<a id="s5"></a>

### 5.1 容量与格式
<a id="s5-1"></a>

- 每个任务邮箱容量: `TE_8R_8051_MAILBOX_CAPACITY` 字节 (默认 4，硬限 15)
- 邮箱缓冲区**必须**在 xdata 中声明
- 信长字段 4 位，0 保留为"空"，实际上限 15 字节

### 5.2 语义: 最新值有效
<a id="s5-2"></a>

旧信未读时新信直接覆盖——邮箱是**管道**而非**队列**，不缓存历史消息。

### 5.3 三个 API 的选择
<a id="s5-3"></a>

| API | 唤醒目标 | 适用上下文 |
|-----|----------|-----------|
| `TE_8R_8051_Send_Mailbox` | 唤醒 (task_delay 清零) | 任务 → 任务 |
| `TE_8R_8051_Post_Mailbox` | 不唤醒 | ISR → 任务 / 预填邮箱 |
| `TE_8R_8051_Read_Mailbox` | — | 仅读本任务邮箱 |

**规则**:

- **ISR 中投递邮箱必须使用 `TE_8R_8051_Post_Mailbox`**，不可使用 `TE_8R_8051_Send_Mailbox`
  - 原因: `TE_8R_8051_Send_Mailbox` 会唤醒目标任务，在 ISR 上下文中触发调度是未定义行为
- `TE_8R_8051_Read_Mailbox` 只读本任务邮箱，不可跨任务读取
- 缓冲不足时不消费邮件 (信保留)，换大缓冲再读
- `dat_p` 参数必须指向 xdata 缓冲区

### 5.4 邮箱 + 延时的典型模式
<a id="s5-4"></a>

```c
void task_consumer(void) large
{
    unsigned char xdata buf[TE_8R_8051_MAILBOX_CAPACITY];

    while (1)
    {
        if (TE_8R_8051_Read_Mailbox(buf, sizeof(buf)) > 0) {
            处理邮件(buf);
        } else {
            TE_8R_8051_Delay(10);    // 无信时睡眠，避免空转
        }
    }
}
```

---

## 六、互斥量规范
<a id="s6"></a>

### 6.1 基本用法
<a id="s6-1"></a>

```c
TE_8R_8051_mutex_t xdata my_mutex;

TE_8R_8051_mutex_init(&my_mutex);      // 初始化 (在 TE_8R_8051_Init 之后)

TE_8R_8051_mutex_acquire(&my_mutex);   // 获取 (可能阻塞)
... 访问共享资源 ...
TE_8R_8051_mutex_release(&my_mutex);   // 释放
```

### 6.2 五条纪律
<a id="s6-2"></a>

| # | 纪律 | 违反后果 |
|---|------|----------|
| 1 | **谁获取谁释放，不可跨任务释放** | `TE_8R_8051_mutex_release` 返回 0，互斥量状态不变 |
| 2 | **同一任务不可重复获取同一互斥量** | 自锁检测: 重复获取直接返回，不阻塞 (但也不获取) |
| 3 | **多互斥量时，获取顺序须一致** | 否则死锁，内核不检测 |
| 4 | **禁止嵌套持有** | 释放时恢复 `orig_prio` 会丢失其他互斥量的继承提升，导致优先级反转窗口 |
| 5 | **不可在 ISR 中调用** | ISR 中无任务上下文，`current_task` 语义不适用 |

### 6.3 纪律 4 详解: 禁止嵌套持有
<a id="s6-3"></a>

**嵌套持有** (禁止):

```c
TE_8R_8051_mutex_acquire(&M1);
    TE_8R_8051_mutex_acquire(&M2);    // ← 此时同时持有 M1 + M2
        ...
    TE_8R_8051_mutex_release(&M2);    // ← 释放 M2 时恢复 orig_prio，M1 的继承提升丢失
TE_8R_8051_mutex_release(&M1);
```

**顺序持有** (正确):

```c
TE_8R_8051_mutex_acquire(&M1);
    ...
TE_8R_8051_mutex_release(&M1);       // ← M1 已释放，当前无锁

TE_8R_8051_mutex_acquire(&M2);       // ← 只持有 M2
    ...
TE_8R_8051_mutex_release(&M2);
```

### 6.4 优先级继承行为
<a id="s6-4"></a>

- 当高优先级任务阻塞在互斥量上时，持有者的有效优先级被临时提升到等待者中最高的优先级
- 释放时恢复原始优先级，唤醒最高优先级等待者
- 继承仅基于静态优先级，不含饥饿老化值 (继承是优先级关系，老化是调度公平性，语义不同)

### 6.5 等待关系存储
<a id="s6-5"></a>

- 等待关系存储在互斥量侧的 `waiters[]` 位图数组中：`waiters[i/8]` 的 `bit(i%8)=1` 表示任务i正在等本锁
- 位图数组长度自适应：`TE_8R_8051_WAITER_WORDS = (MAX_TASKS+7)/8`
  - MAX_TASKS=1~8 → 1字节，MAX_TASKS=9~16 → 2字节，MAX_TASKS=17~24 → 3字节
- 4任务配置下与旧版单字节位图完全一致，RAM零增长
- 位图方案消灭了旧版指针数组的 NULL 与 xdata 0x0000 地址碰撞缺陷
- 任务数不再受位宽硬限，自然扩展

### 6.6 TE_8R_8051_mutex_try_acquire
<a id="s6-6"></a>

非阻塞获取: 互斥量空闲则获取并返回 1，已被占用则返回 0 (不阻塞)。可在轮询场景中使用。

---

## 七、临界区规范
<a id="s7"></a>

### 7.1 三件套用法
<a id="s7-1"></a>

```c
void my_func(void)
{
    TE_8R_8051_CRITICAL_VAR();       // ① 声明保存变量 (放在函数变量区)

    TE_8R_8051_ENTER_CRITICAL();     // ② 保存 EA 并关中断
    ... 被保护代码 ...
    TE_8R_8051_EXIT_CRITICAL();      // ③ 还原 EA
}
```

### 7.2 纪律
<a id="s7-2"></a>

- **每个函数最多一对临界区** — 变量名 `_ea_bak_` 固定，多对会重名报错
- **临界区内提前 return 时，须先手动 `TE_8R_8051_EXIT_CRITICAL()`** — 否则中断永久关闭
- 三件套在任务和 ISR 中均可使用 (保存-还原 EA，而非无条件开中断)

### 7.3 临界区内禁止事项
<a id="s7-3"></a>

| 禁止 | 原因 |
|------|------|
| 调用 `TE_8R_8051_Delay` / `TE_8R_8051_yield` | 中断已关，调度器无法推进时间，死锁 |
| 调用 `TE_8R_8051_mutex_acquire` | 可能阻塞，中断已关，无法被唤醒 |
| 调用 `TE_8R_8051_Send_Mailbox` (唤醒版) | 虽然安全，但唤醒后调度被延迟到开中断后，语义不直观 |

---

## 八、ISR 规范
<a id="s8"></a>

### 8.1 可调用的 API
<a id="s8-1"></a>

| API | ISR 中可用 |
|-----|-----------|
| `TE_8R_8051_Post_Mailbox` | ✅ 静默投递，不触发调度 |
| `TE_8R_8051_CRITICAL_VAR/ENTER/EXIT` | ✅ 保存-还原 EA |
| `TE_8R_8051_mutex_try_acquire` | ⚠️ 技术上可行但不推荐，ISR 中无任务上下文 |

### 8.2 禁止调用的 API
<a id="s8-2"></a>

| API | 原因 |
|-----|------|
| `TE_8R_8051_Delay` | 会触发任务切换，ISR 不是任务 |
| `TE_8R_8051_yield` | 会触发任务切换，ISR 不是任务 |
| `TE_8R_8051_Send_Mailbox` | 唤醒目标任务会触发调度 |
| `TE_8R_8051_mutex_acquire` | 可能阻塞，ISR 不可阻塞 |
| `TE_8R_8051_mutex_release` | 可能触发重调度 |
| `TE_8R_8051_Read_Mailbox` | 依赖 `current_task`，ISR 中无任务上下文 |

### 8.3 ISR 存储模型
<a id="s8-3"></a>

- ISR 函数**必须**声明为 `small`，中断延迟敏感
- ISR 中避免长操作，尽快完成并退出

### 8.4 定时器 0 保留
<a id="s8-4"></a>

- 定时器 0 已被内核占用为系统心跳源，**用户不得使用定时器 0**
- 定时器 0 中断向量 (0x000B) 已被 `Kernel.a51` 占用
- 用户可使用定时器 1 / 定时器 2

---

## 九、存储模型规范
<a id="s9"></a>

| 代码类型 | 存储模型 | 原因 |
|----------|----------|------|
| 任务函数 | `large` | 局部变量在 xdata，idata 栈需求极小 |
| 内核函数 | `small` | 每 Tick 热路径，速度优先 |
| ISR | `small` | 中断延迟敏感 |
| 热循环内层 | 可显式 `small` | 若需极致速度 |
| 邮箱缓冲区 | `xdata` | API 指针参数为 xdata 专属指针 |
| 任务栈区 | `xdata` | 工作区模式下冻结帧存储在 xdata |
| 互斥量结构体 | `xdata` | `TE_8R_8051_mutex_t xdata` |

---

## 十-A、TCB 冷热分离架构

<a id="s10A"></a>

### 10A.1 设计动机
<a id="s10A-1"></a>

8051 的 C 编译器要求结构体整体存放在同一个存储区，不能"这个字段放 data，那个字段放 xdata"。但任务数据有的必须放 data（汇编热路径高频访问），有的只能放 xdata（不频繁但占空间）。因此分成两个结构体：热 TCB 放 data，冷 TCB 放 xdata。

### 10A.2 结构体定义
<a id="s10A-2"></a>

**热 TCB** (`TE_8R_8051_tcb_hot_t`，data 区，5 字节/槽):

| 字段 | 原分散数组 | 访问频率 |
|------|-----------|---------|
| `sp` | `task_sp[]` | 每次切换读写（最热） |
| `meta` | `task_meta[]` | 每 Tick 读写 |
| `xstack_dpl` | `task_xstack_dpl[]` | 每次切换读 |
| `xstack_dph` | `task_xstack_dph[]` | 每次切换读 |
| `stack_guard` | `task_stack_guard[]` | 每次切换读 + Tick 读 |

**冷 TCB** (`TE_8R_8051_tcb_cold_t`，xdata 区，3 字节/槽):

| 字段 | 原分散数组 | 访问频率 |
|------|-----------|---------|
| `delay` | `task_delay[]` | Tick 遍历读写 |
| `starve` | `task_starve[]` | 每 Tick 遍历读写 |

### 10A.3 数组声明
<a id="s10A-3"></a>

```c
TE_8R_8051_tcb_hot_t  data  tcb_hot[TE_8R_8051_SLOT_COUNT];   // 含空闲任务
TE_8R_8051_tcb_cold_t xdata tcb_cold[TE_8R_8051_MAX_TASKS];   // 不含空闲任务
```

### 10A.4 汇编访问方式
<a id="s10A-4"></a>

TCB 化热分离后，汇编访问 `tcb_hot[i].field` 的地址计算：

```
地址 = TCB_HOT + i × sizeof(TE_8R_8051_tcb_hot_t) + offsetof(field)
     = TCB_HOT + i × 5 + offsetof(field)
```

其中 `i × 5` 通过 `MUL AB` 指令实现（2 机器周期），字段偏移通过 `INC R0` 定位。

### 10A.5 RAM 开销（4 任务配置）
<a id="s10A-5"></a>

| 存储区 | 对象 | 大小 |
|--------|------|------|
| data | `tcb_hot[5]` | 25 字节 |
| xdata | `tcb_cold[4]` | 12 字节 |

与分散数组方案完全一致，RAM 零增长。

---

## 十、栈溢出诊断
<a id="s10"></a>

### 10.1 三层检测机制
<a id="s10-1"></a>

| 层级 | 时机 | 检查内容 | 防护方向 |
|------|------|---------|---------|
| L1 | task_switch / Timer0_Isr 入口，PUSH 13寄存器之前 | `(SP_now - WORKSPACE) - (stack_guard - 11) ≥ 0` | 防止 PUSH 本身踩哨兵 |
| L2 | 调度器选出 next 任务后，搬栈恢复之前 | next 任务 xdata 栈中哨兵 ≠ 0xFE | 防止派发已溢出任务 |
| Tick | 每 1ms Tick 中断 | `workspace[stack_guard] ≠ 0xFE` | 检测运行中栈增长踩哨兵 |

- 创建任务时在 xdata 栈区天花板埋设哨兵 (`0xFE`)
- 任意一层检测到溢出后: 停心跳 → 关中断 → 短路至空闲任务死循环停机

### 10.2 诊断变量
<a id="s10-2"></a>

溢出停机后，通过调试器读取 6 个诊断变量（全部为 `unsigned char data`）：

| 变量 | 含义 |
|------|------|
| `TE_8R_8051_stack_overflow_flag` | 非零表示溢出发生 |
| `TE_8R_8051_stack_overflow_id` | 哪个任务溢出 |
| `TE_8R_8051_stack_overflow_sp` | 检测时刻 SP 寄存器绝对值 |
| `TE_8R_8051_stack_overflow_task_sp` | 溢出任务冻结时栈顶偏移量 (反映栈深度) |
| `TE_8R_8051_stack_overflow_guard_offset` | 溢出任务哨兵在工作区中的偏移量 |
| `TE_8R_8051_stack_overflow_magnitude` | 溢出幅度 = task_sp - stack_guard |

推算: 哨兵与栈顶间距 = `stack_guard - task_sp`，间距越小，溢出时栈距天花板越近。

### 10.3 可靠性边界
<a id="s10-3"></a>

| 溢出程度 | L1 | L2 | Tick | 诊断值正确性 | 停机 |
|----------|----|----|------|-------------|------|
| 轻度/中度 (踩哨兵或部分用户变量) | ✅ | ✅ | ✅ | ✅ 正确 | ✅ 成功 |
| 重度 (踩 `tcb_hot[].sp` 等内核数据) | ✅ | ✅ | ✅ | ⚠️ `sp` 值可能错 | ✅ 成功 |
| 灾难级 (踩 idle_task 栈或调度器核心) | ❌ | ❌ | ❌ | — | — |

第三行是 8051 无 MMU 的物理极限，任何纯软件方案都无法突破。

### 10.4 停机后指示
<a id="s10-4"></a>

可在 `idle_task` 的 `while(1)` 中添加硬件指示:

```c
void idle_task(void)
{
    while (1) {
        if (TE_8R_8051_stack_overflow_flag) {
            P12 = 1;    // 拉引脚 / 闪 LED 等
        }
    }
}
```

---

## 十一、空闲任务规范
<a id="s11"></a>

- 空闲任务是内核内部兜底哨兵，**用户不可见、不可操作**
- 空闲任务永远就绪、优先级恒为 0、不睡眠、不使用邮箱、不参与饥饿老化
- 空闲任务栈 36 字节 (初始帧 15 字节 + 中断帧 17 字节 + 哨兵 1 字节 + 裕量 3 字节)，不可向 `idle_task` 中塞入过多逻辑
- 全员睡眠时 CPU 自动进入空闲任务

---

## 十二、禁止事项汇总
<a id="s12"></a>

| # | 禁止 | 原因 |
|---|------|------|
| 1 | 任务函数使用 `using` 关键字 | 内核保存/恢复 R0~R7 使用第 0 组直接地址，切换寄存器组会导致数据丢失 |
| 2 | 任务函数返回 (非死循环) | 调度器假设任务永不退出，返回后执行流未定义 |
| 3 | 嵌套持有互斥量 | 释放时恢复 `orig_prio` 丢失其他互斥量的继承提升 |
| 4 | ISR 中调用 `TE_8R_8051_Delay` / `TE_8R_8051_yield` / `TE_8R_8051_mutex_acquire` / `TE_8R_8051_mutex_release` / `TE_8R_8051_Send_Mailbox` / `TE_8R_8051_Read_Mailbox` | ISR 无任务上下文或会触发调度/阻塞 |
| 5 | 使用定时器 0 | 已被内核占用为系统心跳源 |
| 6 | 临界区内调用 `TE_8R_8051_Delay` / `TE_8R_8051_yield` / `TE_8R_8051_mutex_acquire` | 中断已关，无法调度/唤醒，死锁 |
| 7 | 临界区内提前 return 未先 `TE_8R_8051_EXIT_CRITICAL()` | 中断永久关闭 |
| 8 | 邮箱缓冲区声明在非 xdata 区 | API 指针参数为 xdata 专属指针 |
| 9 | 跨任务释放互斥量 | `TE_8R_8051_mutex_release` 校验 `owner != current_task` 会返回 0 |
| 10 | 多互斥量获取顺序不一致 | 死锁，内核不检测 |

---

## 十三、典型任务模板
<a id="s13"></a>

### 13.0 完整创建与启动示例
<a id="s13-0"></a>

以下示例展示如何声明栈区、编写任务函数、在 `main` 中创建任务并启动内核：

```c
#include "TE_8R_8051.h"

/* ① 声明栈区（必须在 xdata 中） */
unsigned char xdata stack_sensor[42];    // 任务0栈，42 字节
unsigned char xdata stack_ctrl[42];      // 任务1栈，42 字节

/* ② 编写任务函数（必须 large，必须死循环） */
void task_sensor(void) large
{
    while (1)
    {
        读取传感器();
        TE_8R_8051_Delay(10);            // 每 10ms 执行一次
    }
}

void task_ctrl(void) large
{
    while (1)
    {
        控制逻辑();
        TE_8R_8051_Delay(20);            // 每 20ms 执行一次
    }
}

/* ③ main 中创建任务并启动 */
void main(void)
{
    硬件初始化();                        // UART、GPIO 等

    TE_8R_8051_Init();                   // 内核初始化

    TE_8R_8051_Task_Create(0, 5, task_sensor, stack_sensor, sizeof(stack_sensor));
    TE_8R_8051_Task_Create(1, 8, task_ctrl,    stack_ctrl,    sizeof(stack_ctrl));

    TE_8R_8051_Start();                  // 启动调度，永不返回
    while (1);                           // 防编译警告
}
```

**`Task_Create` 五个参数**：

| 参数 | 含义 | 示例 |
|------|------|------|
| `id` | 任务编号（0 ~ MAX_TASKS-1），即物理槽位 | `0` |
| `prio` | 优先级（1~15，0 自动升为 1） | `5` |
| `task` | 任务函数指针 | `task_sensor` |
| `stack` | xdata 栈区首地址 | `stack_sensor` |
| `stack_size` | 栈区大小（≥33，≤MAX_WORKSPACE） | `sizeof(stack_sensor)` 即 `42` |

**调用顺序不可颠倒**：`Init` → `Task_Create × N` → `Start`。

### 13.1 周期采样任务
<a id="s13-1"></a>

```c
unsigned char xdata stack_sensor[42];

void task_sensor(void) large
{
    unsigned char xdata sample[4];

    while (1)
    {
        读取传感器(sample);
        TE_8R_8051_Send_Mailbox(1, sample, 4);    // 发给处理任务
        TE_8R_8051_Delay(10);                      // 每 10ms 采样一次
    }
}
```

### 13.2 互斥保护任务
<a id="s13-2"></a>

```c
unsigned char xdata stack_comm[42];
TE_8R_8051_mutex_t xdata uart_mutex;

void task_comm(void) large
{
    while (1)
    {
        TE_8R_8051_mutex_acquire(&uart_mutex);
        Uart_SendString("hello\r\n");
        TE_8R_8051_mutex_release(&uart_mutex);

        TE_8R_8051_Delay(100);
    }
}
```

### 13.3 邮箱消费任务
<a id="s13-3"></a>

```c
unsigned char xdata stack_ctrl[42];

void task_ctrl(void) large
{
    unsigned char xdata mail[TE_8R_8051_MAILBOX_CAPACITY];
    unsigned char n;

    while (1)
    {
        n = TE_8R_8051_Read_Mailbox(mail, sizeof(mail));
        if (n > 0) {
            处理数据(mail, n);
        } else {
            TE_8R_8051_Delay(5);    // 无信时睡眠，避免空转
        }
    }
}
```

### 13.4 ISR 投递邮箱
<a id="s13-4"></a>

```c
void Timer1_Isr(void) interrupt 3
{
    unsigned char xdata tmp[2];

    读取硬件(tmp);
    TE_8R_8051_Post_Mailbox(0, tmp, 2);    // 静默投递，不触发调度
}
```

---

## 十四、任务状态谓词宏
<a id="s14"></a>

### 14.1 四个谓词
<a id="s14-1"></a>

| 谓词宏 | 含义 | delay值域 |
|--------|------|-----------|
| `TE_8R_8051_IS_TOMBSTONE(i)` | 槽位未使用（墓碑） | == 0xFFFF |
| `TE_8R_8051_IS_BLOCKED(i)` | 阻塞态（等互斥量） | == 0xFFFE |
| `TE_8R_8051_IS_READY(i)` | 就绪态（可上CPU） | == 0 |
| `TE_8R_8051_IS_SLEEPING(i)` | 睡眠态（等时间） | 1 ~ 65533 |

### 14.2 使用场景
<a id="s14-2"></a>

- 调度器：排除不可候选的状态（墓碑/阻塞/睡眠）
- Tick处理：只给睡眠态减延时
- 邮箱唤醒：不唤醒墓碑和阻塞态任务
- 饥饿记账：只给就绪态计饥饿

### 14.3 纪律
<a id="s14-3"></a>

- **内核代码必须使用谓词宏**，禁止裸写 `tcb_cold[i].delay == 0xFFFF` 等魔数判断
- **用户代码一般不需要直接使用谓词宏**——用户通过API（Delay/acquire/release）间接改变任务状态
- 谓词宏定义在 `TE_8R_8051_internal.h`，属于内部实现细节，不在公开API头文件中
- **`IS_SLEEPING` 宏两次读取 `tcb_cold[i].delay`（16位变量），必须在 EA=0 环境下使用**（ISR上下文或临界区内），否则两次读取之间可能被中断打断导致撕裂读取

---

## 十五、汇编-C契约
<a id="s15"></a>

### 15.1 编译时断言
<a id="s15-1"></a>

内核在 `TE_8R_8051_internal.h` 中声明了两个编译时断言：

| 断言 | 含义 |
|------|------|
| `sizeof(TE_8R_8051_tcb_hot_t) == 5` | 热TCB结构体大小必须为5字节（汇编用 `MUL AB(#05H)` 算偏移） |
| `TE_8R_8051_ISR_FRAME_SIZE == 17` | 中断帧大小必须为17字节（汇编L1检测用 `#0BH` 做边界计算） |

如果C侧修改导致这些假设不成立，编译直接报错（负数数组大小），不会带着错误编译通过。

### 15.2 汇编常量同步
<a id="s15-2"></a>

| C侧 (config.h) | 汇编侧 (config.inc) | 同步要求 |
|-----------------|---------------------|---------|
| `TE_8R_8051_MAX_TASKS` | `TE_8R_8051_MAX_TASKS EQU` | 值必须相同 |
| `TE_8R_8051_STACK_MAGIC` | `TE_8R_8051_STACK_MAGIC EQU` | 值必须相同 |

C侧的编译时断言无法覆盖汇编侧的常量——汇编没有 `sizeof`。因此同步依赖开发者手动维护，config.inc 文件头已标注修改纪律。

---

## 十六、源文件清单
<a id="s16"></a>

| 文件 | 语言 | 职责 |
|------|------|------|
| `TE_8R_8051_config.h` | C | 用户可配置参数 |
| `TE_8R_8051_config.inc` | 汇编 | 汇编侧常量（与config.h同步） |
| `TE_8R_8051.h` | C | 公开API声明 + 数据结构 |
| `TE_8R_8051_internal.h` | C | 内部宏 + 谓词宏 + 编译时断言 |
| `TE_8R_8051_core.c` | C | 内核核心：调度器 + Tick处理 + 任务管理 |
| `TE_8R_8051_mailbox.c` | C | 邮箱：Send/Post/Read |
| `TE_8R_8051_mutex.c` | C | 互斥量：init/acquire/release/try_acquire |
| `Kernel_8051.a51` | 汇编 | 上下文切换 + 中断入口 + 栈溢出L1/L2检测 |